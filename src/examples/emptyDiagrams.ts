import { createEmptyDiagram } from '../model/constructors.ts'

export const emptyTwoDimensionalDiagram = createEmptyDiagram({
  ambientDimension: 2,
})

export const emptyThreeDimensionalDiagram = createEmptyDiagram({
  ambientDimension: 3,
})
